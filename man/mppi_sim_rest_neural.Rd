% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mppi_sim_rest.R
\name{mppi_sim_rest_neural}
\alias{mppi_sim_rest_neural}
\title{Simulate low-rank neural time-series with known modulators}
\usage{
mppi_sim_rest_neural(
  T = 600L,
  r = 6L,
  contexts = c("ctx1", "ctx2"),
  amp = rep(0.4, length(contexts)),
  freq = seq(0.01, length.out = length(contexts), by = 0.005),
  noise_sd = 0.02,
  seed = NULL,
  templates = NULL
)
}
\arguments{
\item{T}{Number of time points.}

\item{r}{Latent dimensionality (components).}

\item{contexts}{Character vector of context names.}

\item{amp}{Vector of coupling amplitudes per context.}

\item{freq}{Vector of temporal frequencies (in cycles / TR) for modulators.}

\item{noise_sd}{Standard deviation of additive white noise on latent series.}

\item{seed}{Optional seed for reproducibility.}

\item{templates}{Optional list of r x r matrices (one per context) specifying
the coupling templates. Each matrix is symmetrised and scaled to unit
Frobenius norm. When omitted, random symmetric templates are generated.}
}
\value{
List with latent series \code{U}, modulators, and true coupling matrices.
}
\description{
Generates latent component activity that depends linearly on supplied
oscillator-like modulators. The simulator constructs modulators with zero mean
and unit variance and injects symmetric coupling patterns so that
\code{mppi_rest()} can recover them.
}
