% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mppi_seedset.R
\name{mppi_seedset_map}
\alias{mppi_seedset_map}
\title{Collapse seed-set slopes into a single map}
\usage{
mppi_seedset_map(
  fit,
  seeds,
  k,
  aggregate = c("svd", "mean", "precision"),
  target_idx = NULL,
  mode = c("normalized", "amplitude", "raw"),
  Theta0 = NULL,
  lag = 0L
)
}
\arguments{
\item{fit}{\code{mppi_fit} object.}

\item{seeds}{Seed specification (as in \code{\link[=mppi_seedset_slopes]{mppi_seedset_slopes()}}).}

\item{k}{Condition/contrast name or index.}

\item{aggregate}{Aggregation rule: \code{"svd"}, \code{"mean"}, or \code{"precision"}.}

\item{target_idx}{Optional target indices (defaults to all locations).}

\item{mode}{Scale for the interaction matrix (\code{"normalized"}, \code{"amplitude"}, \code{"raw"}).}

\item{Theta0}{Optional baseline precision matrix (component or voxel space) for
\code{aggregate = "precision"}.}

\item{lag}{Optional lag (offset) to evaluate; defaults to contemporaneous (0).}
}
\value{
Object of class \code{mppi_seedset_map} containing the aggregated map and weights.
}
\description{
Collapse seed-set slopes into a single map
}
